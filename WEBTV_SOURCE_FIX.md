# WebTV 点播线路修复

本次修复针对首页有推荐/封面但所有影片均提示“没有匹配到可播放线路”的问题。

根因：V2 默认只配置了七七资源 `https://www.qiqidys.com/api.php/provide/vod` 作为网页点播 CMS，一旦该单源不可用，所有影片搜索都会返回空结果。

修复内容：

- 默认点播源改为多源并发：极速资源、爱奇艺资源、速播资源。
- 搜索使用 `Promise.allSettled`，单个源失败不会拖垮其他源。
- 增加标题清洗与季数回退搜索，提高中文标题匹配率。
- 搜索结果按标题相似度与年份排序。
- 搜索与详情共用完全一致的源 key/api 清单，避免“搜索成功但详情未知来源”。
- 详情增加超时、JSON 校验与播放线路解析。
- 新增 `/webtv/sources` 健康检查接口，可直接查看每个 CMS 源是否正常及响应耗时。

如需自定义源，仍可通过 Cloudflare Pages 环境变量 `WEBTV_CMS_SOURCES` 覆盖默认多源。